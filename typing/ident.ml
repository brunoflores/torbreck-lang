(* open Utils.Local_store *)

(* type t = Local of { name : string; stamp : int } *)
(* (\* Field stamp is for fast comparison. *\) *)

(* let currentstamp = s_ref 0 *)
(* let predefstamp = s_ref 0 *)

type t = int
